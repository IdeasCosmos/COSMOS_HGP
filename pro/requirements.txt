# COSMOS-HGP PRO API Server 종속성
# Python 3.8+ 필요

# 웹 프레임워크
fastapi==0.104.1
uvicorn[standard]==0.24.0

# 데이터 검증
pydantic==2.5.0

# 수치 계산
numpy==1.24.3
pandas==2.0.3

# 파일 업로드
python-multipart==0.0.6

# HTTP 클라이언트 (선택사항)
httpx==0.25.2

# 로깅 (선택사항)
loguru==0.7.2

# 환경 변수 (선택사항)
python-dotenv==1.0.0

# 테스트 (개발용)
pytest==7.4.3
pytest-asyncio==0.21.1